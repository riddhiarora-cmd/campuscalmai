# Client (Just Breathe)

Simple React + Vite client to preview the `Just Breathe` component.

Run locally from the repo root (requires dependencies):

```bash
npm install
npm run web:dev
```

Open http://localhost:5173
